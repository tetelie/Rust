use std::collections::HashMap;
use rand::Rng;

use std::{cmp::Ordering, io};
use std::io::{self, Write};

use std::collections::*;


fn main() {
    let mut map = HashMap::new();
    map.insert(1, 2);
}

use std::fmt;
use std::io;

fn fonction1() -> fmt::Result {
    // -- partie masquée ici --
    Ok(())
}

fn fonction2() -> io::Result<()> {
    // -- partie masquée ici --
    Ok(())
}

use std::fmt::Result;
use std::io::Result as IoResult;

fn fonction12() -> Result {
    // -- partie masquée ici --
    Ok(())
}

fn fonction22() -> IoResult<()> {
    // -- partie masquée ici --
    Ok(())
}
